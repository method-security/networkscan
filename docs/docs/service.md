# Service

The `networkscan discover service` command identifies and fingerprints network services running on a specific port of a target host.

## Usage

```bash
networkscan discover service --target scanme.sh --port 443
```

For full help information, run:

```bash
networkscan discover service -h
```
