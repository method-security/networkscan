# Bruteforce

The `networkscan pentest bruteforce` command performs a brute force attack against a target service.

## Usage

```bash
networkscan pentest bruteforce --targets 192.168.0.10:22 --module ssh
```

For full help information, run:

```bash
networkscan pentest bruteforce -h
```
