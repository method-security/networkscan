# TLS

The `networkscan discover tls` command retrieves and analyzes TLS configuration and certificate details from one or more targets.

## Usage

```bash
networkscan discover tls --targets example.com:443
```

For full help information, run:

```bash
networkscan discover tls -h
```
