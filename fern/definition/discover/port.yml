types:
  PortScanDetails:
    properties:
      port: integer
      protocol: string
  HostScanDetails:
    properties:
      host: string
      ip: string
      ports: optional<list<PortScanDetails>>
  PortScanReport:
    properties:
      hosts: optional<list<HostScanDetails>>
      errors: optional<list<string>>